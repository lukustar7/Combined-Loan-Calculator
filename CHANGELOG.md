# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 与 [Semantic Versioning](https://semver.org/lang/zh-CN/) 规范。

## [1.5.2] - 2026-09-04

### Fixed

- **金融计算核心加固**：修正多次提前还款混合策略下的有效剩余期数记忆机制，彻底杜绝“缩短期限后再减少月供”时剩余期数反弹回初始期数的缺陷；新增常规月供峰值指标 `peakRegularPayment`，与包含提前还款的单月最大支出解耦。
- **人民币大写金额转换**：修正跨节连零（如“壹亿零捌仟元整”）时的漏“零”缺陷，并清理多余正则死代码。
- **调色板配置纠偏**：修正 Palette 20 中第 6 种颜色配置笔误（修正为暗金泥黄），杜绝第 3 笔与第 6 笔贷款同色粘连。
- **系统安全与 ID 防御**：加固 `sanitizeLoanId`，强制拦截 `summary`、`__total__` 及 JavaScript 对象原型保留字，根除幽灵贷款漏洞。
- **数据损坏容灾救援**：完善 `initApp` 读取机制，若读取本地存储发生数据异常，自动建立快照备份至安全分区并弹出明确提示，防止用户宝贵数据被静默覆写丢失。
- **防抖时序与生命周期安全落盘**：扩展防抖工具函数支持 `.flush()` 强制落盘，在页面卸载（`beforeunload`）、切后台（`pagehide` 与 `visibilitychange`）以及导出 CSV 前强制同步数据，彻底杜绝在输入后 150ms 窗口内关闭标签页导致数据丢失。
- **移动端真机体验硬伤深度修复**：
  - 修复图表下拉框字体为 `16px`（高度 `36px`），彻底根除 iOS Safari 聚焦下拉框时强制把整屏放大 1.3 倍的系统顽疾；
  - 修复移动端模态框为弹性垂直滚动（`-webkit-overflow-scrolling: touch`），彻底解决软键盘弹起时操作按钮被截断且无法滚动的严重缺陷；
  - 提升任务栏与开始按钮触控高度至 `28px`，结合安全区避让，杜绝误触 iOS 底部 Home Indicator 横条；
  - 移动端横向贷款标签滑动栏增加 `overscroll-behavior-x: contain;`，消除滑动到边界时误触系统后退手势。
- **双主题视觉与交互细节修复**：
  - 修复 Google Material 3 模式下模态框标题栏及圆形关闭按钮被全局选择器错误隐藏的缺陷；
  - 修复 Windows 98 移动端窗口顶部安全区避让，彻底消除与 iPhone 灵动岛和刘海屏的重叠；
  - 模态框打开时自动为 body 增加 `win-modal-open` 锁定背景滚动穿透，并注入 160ms 优雅淡入动效与毛玻璃遮罩；自动聚焦弹窗首项交互元素。
- **极端小屏（<=360px）自适应与无障碍**：
  - 核心指标网格在 360px 及以下超窄屏自适应降级为单列布局，并为数字增加 `overflow-wrap: anywhere;` 彻底防止长数字溢出；
  - 全局补齐 `:focus-visible` 高对比度焦点环（Win98 经典虚线框与 Material 3 现代蓝圈），大幅提升键盘无障碍操作体验。

### Added

- **离线秒开与分层 PWA 缓存**：新增 `sw.js` 实现生产级分层缓存策略（导航 HTML 实行 Network-First 确保在线即时更新且离线秒级回退，静态资源实行 Cache-First 极速秒开）；完善 `manifest.json` 规范属性并支持自适应遮罩图标（Maskable Icons）。
- **极速响应与防抖优化**：引入通用防抖函数，单笔表单编辑打字时实现 150ms 异步防抖，并实行“隐藏大盘零渲染原则”与 Chart.js 实例单例复用增量刷新（`trendChart.update('none')`），杜绝频繁重绘 Canvas 导致的输入掉帧。
- **无障碍访问与移动端原生键盘**：为所有表单输入建立标准 `label for` 关联，金额输入配置 `inputmode="decimal"`、期数配置 `inputmode="numeric"` 唤起纯数字键盘，补齐所有图标按钮 `aria-label` 与表格语义结构（`caption` 与 `th scope`），新增全局键盘 `Escape` 快捷关闭对话框能力。

## [1.5.1] - 2026-08-20

### Added

- 新增太鼓达人画风（Taiko Style）专属 App 图标系列（包含 `apple-touch-icon.png`, `icon-192.png`, `icon-512.png`, `favicon.png`, `favicon.ico`），采用“戴瓦房屋顶手捧算盘的可爱金币君”拟人化主角，并通过 Flood Fill 算法实现 100% 满铺正方形纯色背景，彻底消除移动端系统裁切时的白角瑕疵。
- 新增 PWA Web App Manifest (`manifest.json`) 配置，精简手机桌面应用名称为**「合贷计算」**，彻底解决添加到 iOS/Android 主屏幕时标题过长被省略号截断的问题。
- 深度适配 iOS Safari 安全区（Safe Area Insets，支持 `viewport-fit=cover` 与 `env(safe-area-inset-*)`），彻底解决顶部刘海/灵动岛/状态栏重叠模糊及底部地址栏遮挡问题。
- 新增移动端横向滑动手势贷款导航胶囊栏（Scrollable Horizontal Segmented Bar），释放手机屏幕垂直空间，支持点击自动居中平滑滚动。
- 新增移动端表单防缩放配置（输入框与选择器设置 `16px` 字体），彻底杜绝 iOS Safari 聚焦输入时的强制放大与页面跳动。
- 新增还款方式与提前还款策略专属独立切换按钮组（Toggle Group），彻底根治移动端单选文字折行与圆圈错位问题。

### Changed

- 移动端重塑为自然流畅的流式上下滚动布局，解除 `height: 100vh; overflow: hidden;` 导致的屏幕截断与滑动卡死。
- 优化移动端核心指标卡片为 2 列自适应网格（总额大卡片高亮），提升小屏视觉层级与数字辨识度。
- 优化移动端图表控制栏自适应与表格横向滑动手势。
- 优化全站移动端交互按钮最小触控热区（>= 38px/40px），符合 Apple HIG 人机交互规范。

## [1.5.0] - 2026-08-18

### Added

- 新增首页图表“单笔走势对比”模式（默认开启）：等额本息呈现绝对水平横线，等额本金呈现倾斜递减直线，同时追加高亮“合并总月供”总负荷走势曲线；保留“组合堆叠构成”模式供用户切换。
- 新增 Google Material 3 专属现代顶栏（Header App Bar）与独立卡片侧边栏，支持一键在 Material 3 现代 Web App 与 Windows 98 经典复古之间秒切。
- 新增房贷组合（住房公积金贷款 + 商业住房贷款）一键生成模板，自动对齐起贷年月与期限。
- 新增提前还款综合省息效益计算模块与可视化高亮卡片，直观呈现累计节省利息金额与缩短还款月数。
- 新增金额输入实时中文大写与万元换算辅助胶囊（如 `= 150.00 万元 (壹佰伍拾万元整)`），消除输入错零风险。
- 扩充图表高对比度调色板至 20 种颜色，彻底杜绝多笔贷款堆叠图中的同色粘连现象。
- 新增针对省息效益测算、中文大写金额算法与 20 色调色板的 3 项自动化测试，测试套件扩充至 14 项。

### Changed

- 统一侧边栏导航图标槽位与文字排版系统，去除多余 Emoji 与加号，实现图标垂直绝对居中对齐、文字起始线绝对对齐。
- 彻底重构 Google Material 3 现代质感主题：在 M3 模式下隐藏 Win98 拟物任务栏、开始菜单与窗口控制按钮，切换为标准现代全屏 Web App 体验，彻底消除“Windows 贴皮拼凑感”。
- 重塑全站文案，彻底移除所有 `.exe`, `.cfg`, `.sys`, `.lnk` 等刻意伪装成文件后缀的字样，回归自然纯粹的金融工具文风。
- 金融报表与明细计划表中的所有金额、本金、利息与余额数据列全面改为靠右对齐，并启用 `tabular-nums` 等宽数字排版。
- 极简化关于弹窗文案，仅保留产品名称、版本号与 MIT 开源协议说明。
- 规范重置版本号序列为 `v1.5.0`。

### Fixed

- 修复提前还款配置保存及单笔参数修改后省息效益卡片与大盘指标未即时联动的 Bug，建立全链路实时响应式刷新管道。
- 修复图表在堆叠模式下累加前置斜率导致等额本息曲线被画成斜线的问题，单笔走势现精准反映各自的真实还款轨迹。

### Removed

- 移除 Google Material 3 顶栏与 Windows 98 开始菜单中的房贷组合冗余按钮，仅保留侧边栏唯一标准入口。
- 彻底移除关于弹窗中的冗长功能介绍及版权年份声明。
- 彻底移除多语言（i18n）字典及运行时动态 DOM 扫描替换逻辑，全站精简为纯净简体中文，`app.js` 体积缩减逾 500 行。
- 彻底移除 Windows Vista Aero 毛玻璃样式及相关冗余代码，样式系统大幅精简。
- 移除区域与语言设置弹窗及其关联配置。

## [1.3.4] - 2026-07-06

### Added

- 新增本地固定版 `vendor/chart.umd.min.js`，图表渲染不依赖外部网络与 CDN。
- 新增 Chart.js 加载失败降级视图。

### Changed

- 重构贷款数据清洗入口，统一限制贷款数量、金额、利率、期限、年月与 ID。
- 优化合并计算数据查找，预先映射月度数据为 `Map`。

### Fixed

- 修复异常本地缓存导致初始化白屏的风险。
- 修复单次提前还款向多次提前还款字段迁移不一致的问题。

### Security

- 为 CSV 导出增加公式注入防护，对 `=`, `+`, `-`, `@` 起始单元格进行转义。
- 对用户贷款名称进行 DOM 转义与原型链污染拦截。

## [1.3.3] - 2026-06-17

### Changed

- 优化主题窗口、标题栏、按钮与禁用态样式一致性。

### Fixed

- 修复图表 Y 轴刻度在窄数据范围下失真贴底的问题。

## [1.3.2] - 2026-06-16

### Added

- 新增多次提前还款配置管理器，支持缩短期限与减少月供两种处理方式。
- 新增按月明细与按年汇总图表视图切换。

## [1.2.2] - 2026-06-08

### Added

- 新增提前还款“减少月供”处理方式与图表标记。

### Fixed

- 限制最长还款期限为 3600 期，防御异常大期限卡死主线程。

## [1.2.1] - 2026-06-08

### Added

- 新增显示属性弹窗与外观方案切换。

## [1.1.1] - 2026-06-05

### Changed

- 空数据时自动恢复默认贷款，避免界面留白。

## [1.1.0] - 2026-06-01

### Added

- 新增提前还款模拟器、CSV 导出与金融数字千分位格式化。

## [1.0.0] - 2026-05-31

### Added

- 首次发布 Windows 98 经典复古风格多笔贷款组合合并计算器。
- 支持等额本息与等额本金计算核心、时间线对齐与未来趋势图表。
