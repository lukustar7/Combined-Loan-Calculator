# 📜 变更日志 (CHANGELOG)

本项目的所有重大变更与版本发布日志将在此文档中记录。本日志遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 规范。

---

## [2.2.0-GA] - 2026-06-16

### ➕ 新增功能 (Added)
- **🧮 支持无限多次提前还款配置与属性弹窗管理器**：
  - 重构了底层数据模型与计算公式，废弃单次提前还款逻辑，升级为 `prepayments` 多次还款矩阵，自动检索当月匹配项进行扣减。
  - 支持多次级联还款重算：“减少月供”处理方式在发生多笔提前还款时，能够自动基于当期剩余期数与本金进行多次级联下调重分摊。
  - 新增 Windows 经典复古风格的“提前还款管理器”属性弹窗，支持对多笔还款期数、金额、处理方式进行增删管理与期数升序排序，极大保障了配置体验的连贯与简洁。
  - 自动向下兼容老版本 LocalStorage 数据，在初始化时无损迁移单笔提前还款数据为数组模式。
  - 四语完全同步汉化多次还款管理器的交互指令、校验提示（期数查重、期限范围拦截）与简报渲染。

---

## [2.1.1-GA] - 2026-06-08

### ➕ 新增功能 (Added)
- **🧮 提前还款“减少月供(期限不变)”计算逻辑与选项**：
  - 在“提前还款模拟 (可选)”栏目中新增了“处理方式”单选按钮组，支持“缩短期限”（默认）与“减少月供”两种处理方式。
  - 重构了等额本息 (ACPI) 与等额本金 (ACP) 底层计算公式：当选择“减少月供”并在第 $T$ 期进行部分提前还款时，系统会自动在扣除该期提前还款金额后，计算剩余未还期数 $N_{remain} = N - T$。
  - 从第 $T+1$ 期起，针对剩余本金 $P_{remain}$ 与剩余期数 $N_{remain}$，基于对应还款方式（ACPI/ACP）动态重算未来的每月月供金额，保持原定还款到期日不变，实现完美的“降额”功能。
  - 自动向下兼容已有本地数据，如果旧数据未定义 `prepayMethod`，系统初始化时会自动补全并默认为 `shrink`（缩短期限），确保系统高稳定性。
  - 四种系统语言（简体中文、繁体中文(香港)、英文、日文）完全同步汉化“处理方式”、“缩短期限”和“减少月供”对应的专业金融术语。
- **✨ 新增提前还款图表视觉指示器**：
  - 为了让用户在图表上无需悬停鼠标即可直观获知哪个月份发生了提前还款，编写了 Chart.js 局部渲染插件。当某月份发生提前还款时，自动在柱子最顶端绘制醒目的视觉标记：在 Windows 98/经典深色主题下渲染为经典的**复古红色感叹号警告三角形**，在 Windows Vista 主题下渲染为带高光和外圈阴影的**半透明 Aero 玻璃圆形发光球（内置“P”字符）**。

### ⚙️ 优化与修复 (Changed)
- **📊 智能图表坐标轴与离群值避让 (Chart Axis Optimization)**：
  - 针对大额提前还款可能撑大趋势图 Y 轴上限，导致日常月供波动与趋势线条被极端压扁而不可见的问题，重构了堆叠柱状图数据投射逻辑。
  - 图表柱状体高度现在仅代表“日常常规月供”（当月还款额减去当期提前还款额），自动将大额一次性提前还款金额从柱体高度中剔除，从而让 Y 轴刻度维持在极其健康的常规月供区间，使月供的日常变化趋势、降额与缩期的曲线重获高分辨率可视化价值。
- **⚡ 极佳的垂直交互响应通道 (Interaction Optimization)**：
  - 为了解决鼠标指针或移动端手指需要精确碰触极窄柱子才能激活 Tooltip 的操作不便，并且为解决点击顶部视觉指示标记（红三角或 Aero 小球）时无法响应的问题，重构了 Chart.js 的交互机制（启用 `mode: 'index', intersect: false`）。现在，鼠标悬浮或移动端手指轻触整列垂直柱体范围（包括柱子上方的高空指示器气泡/三角区域）时，均能秒级灵敏激活并呈现该月的 Tooltip 详情，极大提升了可用性与操作连贯性。
  - 在 Chart.js Tooltip（交互提示框）回调中实现了特制提示逻辑，当鼠标悬浮或移动端手指触摸在带有上述指示器的柱体上时，自适应以 4 种语言补充显示 `(当月另有提前还款 XXX 元)`，完美兼顾了整体趋势的可读性、瞬间可读视觉反馈与个别大额支出的信息完整性。
- **📱 移动端固定视口与局部精准滚动 (Mobile Viewport Constraints)**：
  - 彻底修复了手机端（≤768px）因高度 auto 渲染导致滑动时窗口顶栏标题栏、底部任务栏冲出屏幕、移位变形的体验瑕疵。
  - 重构了移动端 CSS 媒体查询：锁死 body 与 desktop 高度为 `100vh` 且限制超出部分（`overflow: hidden`），确保窗口顶部的 Titlebar 与底部 Taskbar 在任何滑动操作下都稳如磐石、永远置于屏幕可视范围内。
  - 主窗口 and 工作区布局升级为高强度 Flex 计算方案，将滚动条局限在最核心的内容工作区（`.win-workspace-content` 与 `.win-tab-content-wrapper`）内部，实现了局部精准垂直滚动，全面杜绝外部溢出，高度还原原生移动操作系统的优雅质感。
  - 同步修复了 Windows Vista 主题下的桌面底部留白，在手机端自适应跟随任务栏厚度（40px）变化，完全杜绝界面底部元素被任务栏遮挡或卡死的问题。

### 🛡️ 安全修复 (Security)
- **🔒 DOM-XSS 安全漏洞加固**：在左侧树形目录（渲染贷款名称）和合并汇总表（渲染活跃贷款名）中，引入 `escapeHTML` 转义函数对用户输入参数进行实体字符强制过滤，防范潜在的 DOM 型与本地存储型脚本注入劫持风险。

### 🐛 Bug 修复 (Fixed)
- **🧮 负利率计算死锁修复**：在单笔贷款计算引擎中强制限制年利率不得为负数（`Math.max(0, rate)`），从根本上避免等额本息公式因分母为 0 导致计算出 `Infinity` 或 `NaN`，防止数值假死及图表崩溃。
- **⚡ 超长还款期限主线程卡死修复**：对贷款期限 `term` 强制引入上限限制（`Math.min(3600, term)`，即最长 300 年 / 3600 期），拦截由于异常输入极长月数导致大循环耗尽 CPU 主线程，避免浏览器内存溢出 (OOM) 与崩溃。

---

## [2.1.0-GA] - 2026-06-08

### ➕ 新增功能 (Added)
- **🖥️ Windows Vista Aero 玻璃主题**：
  - 新增了一套高精度还原的 Windows Vista Aero 配色方案。通过复杂的径向与线性 CSS 混合渐变，实现了经典的 Vista 极光桌面背景（Aurora Wallpaper）。
  - 使用 `backdrop-filter: blur(12px) saturate(150%)` 配以亮白高光描边与半透明白色填充，完美再现了 Vista Aero 经典的“毛玻璃（Aero Glass）”窗口、任务栏、选项卡和弹窗效果。
  - 重塑了开始按钮。在 Vista 主题下，原有的 win98 方形开始按钮自适应升级为经典的半透明圆形开始徽标（Start Orb），悬停时带有呼吸式蓝色发光动画。
  - 重写了窗口标题栏的控制按钮。关闭按钮自适应变为高光亮红色圆角矩形，最小化和最大化按钮变为暗色圆角扁平金属风格，且全局字体自动切换为 Segoe UI。
- **🖥️ 控制面板：显示属性 (Display Properties) 弹窗**：
  - 开始菜单中的“切换深色主题”升级为“显示属性(D)...”。点击可唤起高保真还原的 Windows“显示属性”控制面板。
  - 弹窗中集成了经典的“外观(A)”选项卡，配备了微缩版的“显示器预览框”，可以在应用主题之前实时展示各主题方案的界面配色和窗口外形。
  - 支持“确定”、“取消”、“应用(A)”按钮，实现局部预览与全局应用的解耦，应用的主题配置实时写入 LocalStorage 并自动持久化加载。
- **🌐 区域和语言设置 (Regional Options) 弹窗与繁体中文（香港）/日语支持**：
  - 开始菜单中的“语言切换”升级为“区域和语言设置(L)...”。点击可唤起高还原度的系统语言设置控制面板。
  - 在 I18N 词典中补全了 50+ 个 UI 项的地道繁体中文（香港）和日文翻译。
  - 支持全盘自动适配货币单位（繁体/简体“元”、日文“円”、英文“¥”）以及 formatNumber 的本地数字千分位格式化（'zh-CN' / 'zh-HK' / 'ja-JP' / 'en-US'）切换。

### ⚙️ 优化与修复 (Changed)
- **📊 联动图表自适应主题重绘**：
  - 针对 Windows Vista Aero 主题，重塑了 Chart.js 填充色方案。柱状图改用半透明且带亮色描边的 Aero 蓝、Aero 青、Aero 玫瑰红等 Vista 标志性发光色，并且柱子间距拉开以显现现代质感。
  - 自动更新 Chart.js 全局字体为 Segoe UI，并将轴网格线转换为极轻柔的半透明白色/灰色实线，使大盘走势图表与 Vista 操作系统风格浑然一体。
- **📱 移动端性能与响应式安全兜底**：
  - 在移动端媒体查询下，对 Vista 皮肤的任务栏高度、圆形开始按钮的触控热区、主窗口圆角及内边距进行特制微调，使其在窄屏设备上充当“Windows Vista Phone 概念版”高档紧凑排版，无任何溢出或横向滚动。
  - 针对不支持 CSS backdrop-filter 的旧款移动端设备，加入性能安全兜底，自动降级为半透明单色底而不强制进行高斯模糊计算，确保在千元机 and 老款手机上的滚动流畅度与续航。
- **🐛 修复 Vista 开始按钮文字重叠漏洞**：修复了 Windows Vista Aero 主题下开始按钮未隐藏“开始”中英文字体导致文字在圆形 Start Orb 上错位重叠的视觉缺陷，仅保留经典的圆形 Vista 徽标。
- **🐛 修复 Vista 开始栏文字残留、旋转方向相反与多语言条目丢失漏洞**：
  - 重构了开始菜单左侧栏。在 Windows Vista 主题下隐藏了原先硬编码的 “Windows 98” 文字，通过强制重置父级侧栏的 transform 属性消除二次旋转叠加，配合 absolute 定位和整体逆时针 90 度旋转，将其修正为完美水平居中、从底朝上、字头朝左的 “Windows Vista” 装饰文字。
  - 修复了 I18N 词典由于多语言框架升级导致不慎漏掉的 menuGitHub 和 menuAbout 条目，解决了二级菜单名直接暴露的问题；并且将 menuLang 指示文本修改为全球通用的带“Language”标识词汇，确保懂任意语言的用户都能一眼认出这是语言设置，并能成功将语言切回。
- **🐛 修复多语言环境下快捷名称填充失效漏洞**：重构了 setQuickName 逻辑，抛弃了原本仅能适配中文的硬编码判断，直接联动 I18N 词典提取，使繁体中文（香港）和日语界面下点击快捷命名按钮能正确填入对应的繁体或日语贷款名称。

---

## [2.0.1-GA] - 2026-06-05

### ⚙️ 优化与修复 (Changed)
- **🧮 自动缺省数据填充，杜绝界面留白与异常展示**：优化了空数据拦截逻辑。当用户清空数据库、删除最后一笔配置文件或首次启动遭遇缓存格式异常时，系统在初始化及数据操作环节自动为用户重新注入一笔 100 万元、3.5% 利率的初始默认贷款文件。此举从底层杜绝了空数据下因 `unitWan` 标签未定义而在卡片顶部显示“0.00 unitWan”异常字符的问题，确保计算器始终有计算实体，用户开箱即可直接交互。同时修复了空数据备用状态 `renderEmptyState` 下的单位渲染漏洞（将 undefined 的 `unitWan` 纠正为 `unitYuan`），使降级渲染更加强健。
- **💲 统一中英文币种数值展示**：重构了全局大盘数值的国际化展示逻辑，不再在中英文环境分别采用“万元/元”双重计价标准。即使在中文模式下也直接采用与英文统一的“元（Yuan）”加金融千分位格式化大数展示（例如 1,000,000.00 元），彻底理顺了计算器内部零头级精准输入底座与界面输出的对应关系，使两套语言在数字展示上绝对对称。

## [2.0.0-GA] - 2026-06-01

### ➕ 新增功能 (Added)
- **🧮 提前还款模拟器 (Prepayment Simulator)**：
  - 单笔贷款支持配置在第 $T$ 期还款后，一次性部分提前还本 $A$ 万元。
  - 核心采用经典的“月供不变，期限缩短”算法，自动对剩余期数计算节息和提前结清，并在还款明细中以高亮复古蓝色背景特别标注提前还款行为。
- **🌐 完整多语言国际化 (i18n Localization)**：
  - 提取覆盖全站 50+ 个普通标签与弹窗的互译词典，支持简体中文与 English 热切换。
  - 支持联动翻译左侧资源目录树后缀名（`.sys` / `.cfg`），以及 Chart.js 的轴标签与 legends 图例。
- **🌙 经典黑客深色主题 (Retro Dark Theme)**：
  - 开始菜单新增“切换深色主题”，一键将桌面及窗口切换为炫酷的 Windows 98 经典深灰/黑客蓝绿色皮肤。
  - 联动重绘 Chart.js！动态更改图表刻度文字、点状虚线网格线和 Tooltip 的暗黑皮肤配色，保障完美的黑夜易读性。
- **💾 一键 CSV 导出 (CSV Exporter)**：
  - 全局合并月度还款计划总表和单笔明细表支持一键导出为标准的 `.csv` 文件。
  - 数据流开头注入 `\ufeff` (UTF-8 BOM)，彻底杜绝 Windows 平台 Excel 打开中文字符产生乱码的顽疾，并支持自适应中英文表头。
- **💲 金融数字千分位格式化**：
  - 编写了通用千分位处理器，将还款明细、汇总指标、单笔速览等所有的金额数字格式化为千分位（如 `1,391,903.32`），视觉体验极佳。
- **⚡ 快捷还款期限选择**：
  - 在参数面板中实现 `5年(60月)`、`10年(120月)`、`20年(240月)`、`30年(360月)` 一键快捷设置按钮，改写参数即触发零延迟重算。
- **⚡ 贷款名称快捷选择按钮**：
  - 在参数配置中贷款名称输入框旁，为用户贴心实现了“房贷 / 车贷 / 信用卡 / 消费贷”四个一键快捷命名微型按钮；支持中英双语自适应（在英文版下，各类型词汇自动精炼为 Mortgage / Auto / Card / Consumer 等地道简短表达，保障在复古小按钮内完美排版不撑爆），点击即自动联动改写输入框、内存状态与左侧资源树文件名，大幅提升录入效率。
- **📡 专属 GitHub 仓库直连**：
  - 将开始菜单的 GitHub 链接一键直连到您的专属仓库 `https://github.com/lukustar7/Combined-Loan-Calculator`，去除所有干扰弹窗。

### ⚙️ 优化与修复 (Changed)
- **🚀 双倍计算性能优化**：在全局合并时重用先前 mapping 的还款计划数据，彻底消除对 `calculateSingleLoan` 核心公式的二次重复调用，CPU 运算开销降低 50%！
- **📈 O(1) 级年月偏移直算**：重构 `getMonthYearOffset` 函数，完美消除 while 循环，升级为纯数学整除与求余的 $O(1)$ 算法，大幅提高计算上限和性能。
- **🮮 最后一期数学平账修正**：等额本息/本金最后一期利息单独根据上期末剩余本金按月利率直算，本金取剩余全部本金，平账月供取两者之和。彻底消除了原浮点数精度截断误差导致最后一期产生“负数利息”的重大瑕疵！
- **🌙 深色主题细节完美重构**：修复了原先将白前景变量污染为暗色的问题，重新划定了 `--win-box-bg` 和 `--win-text-muted` 等底色与文字层级变量。从根本上彻底解决了深色主题下“标题栏文字、选中左侧树节点文字”隐形以及“卡片说明标签、新增贷款链接”难以看清的严重视觉缺陷。
- **🌐 彻底杜绝中式英语 'Wan' 的金融尴尬**：重新设计了英文模式下的金额折算逻辑。在英文状态下，三个全局大盘指标自动折算为地道的“元 (¥)”大数显示，彻底化解了拼音拼写的 'Wan' 的尴尬，完美符合西方英美直白展现大额本金的阅读习惯。
- **💲 贷款输入底座升级为“元”**：参数配置中的贷款金额与提前还款金额输入框 the 本金单位由“万元”彻底升级为最基础的“元”。此举解脱了原来无法输入零头（例如 651818 元）的严重限制，支持了 1 元钱起步的零钱级极致精确负债分析沙盘；同时在界面输出层做到了中文大盘优雅换算显示“万元”，英文大盘直观显示“元 (¥)”，兼顾了高精度输入与极致阅读审美的无缝融合。
- **🌐 修复快捷还款期限按钮翻译遗漏**：为四个快捷期限选择按钮（5年/10年/20年/30年）绑定了 `data-i18n` 国际化属性，在英文模式下自适应显示为“5 Yrs / 10 Yrs / 20 Yrs / 30 Yrs”，彻底扫清了未翻译中文字符“年”的视觉漏洞。

## [1.9.8-Beta] - 2026-05-31

### ➕ 新增功能 (Added)
- **🖥️ 经典 Windows 98 视觉样式系统**：
  - 纯 CSS 双重立体 3D 边框，高度还原凹凸浮雕雕刻感。
  - 经典的 Tahoma、Arial 与点阵宋体（SimSun）字体层叠配置，开启像素无抗锯齿优化。
  - 经典青绿色背景桌面（`#008080`）、经典的选项卡组件（Tab Control）和树形目录组件（TreeView）。
  - 带微软经典四色徽标的“开始按钮”与支持动态展开的“开始菜单”。
  - 屏幕底部任务栏，自带**实时走字的小时钟 🕒** 以及当前活跃窗口的任务托盘。
- **📁 通用“资源管理器”多笔贷款管理器**：
  - 左侧树形目录显示多笔贷款的配置文件，格式为 `贷款 1.cfg`、`贷款 2.cfg`。
  - 支持通过“➕ 新增贷款.lnk”动态添加无限笔具有独立配置的贷款。
  - 支持在表单中实时修改配置，并支持一键“🗑️ 销毁本笔贷款.lnk”删除配置。
  - 带有菜单栏（文件、编辑、工具、帮助），可通过“帮助 -> 关于本软件”唤起经典的关于信息弹窗。
- **🧮 零延迟反应式计算核心 (Reactive Loan Core)**：
  - 彻底取消“计算”按钮，所有输入框监听 `input`/`change`，数据变动毫秒级自动重算。
  - 精准实现了**等额本息 (ACPI)** 与 **等额本金 (ACP)** 两套经典的底层数学计算模型。
  - 编写了多笔贷款起止年月对齐合并算法，将不同还款期限、不同利率和首还月份的多个贷款合并到统一的自然月时间线上，自动汇总得出月供大盘。
- **📈 复古像素风系统性能监视器 (Chart.js Retro Skin)**：
  - 整合 Chart.js 绘制直观的多笔贷款未来月供趋势堆叠柱状图。
  - 像素化图表配置：自定义 Tahoma 像素字体、直角黄色悬浮 balloon 框、点状虚线网格线（Dotted Grid Lines）。
  - 采用 Windows 代表色彩作为多笔贷款柱体堆叠配色，一眼看清不同阶段的月供构成占比。

### ⚙️ 优化与修复 (Changed)
- **🛡️ 贷款配置文件上限保护 (20笔最大限制)**：为防止恶性大量创建配置文件导致浏览器 CPU 负载崩溃（大合流计算指数级膨胀），限制系统最大支持 20 笔贷款配置。当达到 20 笔时，左侧树的“新增贷款”自动置灰并更名为 `❌ 新增贷款... (已达20笔上限)`，同时点击会触发标准的 Win98 风格系统安全警告弹窗。
- **🧹 极致精简界面组件**：
  - 移除了窗口顶部重复、臃肿的“文件/编辑/工具/帮助”菜单栏，让管理器窗口 100% 聚焦于核心参数配置。
  - 删除了底部任务栏系统托盘中不具备实际业务功能的音量（🔊）和网络（🖥️）装饰图标，仅保留高精度时钟。
- **🌐 智能 GitHub 仓库直达**：在开始菜单的“访问 GitHub 仓库”项上集成了 `visitGitHub()` 反应式判断机制。在本地运行时自动拦截并弹出部署提示弹窗，当 push 到 GitHub Pages 后，将智能识别并自动生成精确直达用户专属的 `Combined-Loan-Calculator` 源码仓库链接。
- **📝 重构 README.md 说明文档**：根据用户反馈，剔除了文档中所有花哨、热情的修饰性词汇，将其彻底重构为极其冷峻、克制、硬核的 90 年代系统手册技术风格。
- **🎨 修复 macOS 字体发虚破碎问题**：在 Retina 屏幕上强行使用 `none` 渲染非像素字库会导致汉字破碎发虚。我们改用了 macOS 表现最出色的次像素抗锯齿 `subpixel-antialiased`，并将中文字体栈首位更改为经典的苹果宋体（`Songti SC` / `STSong`），在还原 Windows 98 复古雕刻感的同时确保了极高的文字清晰度。
- **🧹 清爽的单笔初始化**：将默认启动时的预设数据精简为一笔“贷款 1”，避免初始化页面过于杂乱。
- **📱 响应式流式布局适配**：在窄屏手机端自适应将左侧树与右侧工作区转换为上下流式堆叠，限制了横轴图表刻度，确保小屏下表单和表格不溢出、易点击。
- **💾 本地浏览器缓存同步**：自动将贷款数组状态序列化存入 `localStorage`，刷新或关闭浏览器后，用户的负债沙盘数据自动恢复，无需担心丢失。
