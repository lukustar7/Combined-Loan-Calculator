{
  "name": "combined-loan-calculator",
  "version": "2.2.2",
  "private": true,
  "type": "module",
  "description": "纯静态多笔贷款组合计算器",
  "scripts": {
    "build": "node --check app.js && node --check src/loan-engine.js && node scripts/validate-static-app.js",
    "test": "node --test",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
